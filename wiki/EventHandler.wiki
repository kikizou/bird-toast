#summary Skeleton for handling input events
#labels Phase-Implementation

Needs to expose actions as strings for configuration using SomeKindOfHash,
relating events to names of hard-coded actions

{{{
class EventTranslation : public XMLObject
{
        Uint8 eventType;
        void** data;     /* array of void pointers to required, xml-configurable event data */
        const char* actionName;
};

/*
 * Takes SDL_Events and handles them based on a virtual function
 */
class EventListener : public XMLObject
{
public:
	bool isActive();
	void setActive(bool state);

	virtual void handleEvent(SDL_Event* e);

        vector<EventTranslation> SomeHash;

private:
	bool active;
};

class KeyListener : public EventListener
{
};

class MouseListener : public EventListener
{
};

}}}


Ex: Each GameScreen will have an EventListener.