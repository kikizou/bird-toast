#summary Skeleton for handling input events
#labels Phase-Implementation

{{{

/*
 * Takes SDL_Events and handles them based on a virtual function
 */
class EventHandler
{
public:
	bool isActive();
	void setActive(bool state);

	void addEvent(SDL_Event* e);
	virtual void handleEvent();


private:
	bool isActive;
	EventQueue events;
};


class KeyHandler : public EventHandler
{
public:
private:
};

class MouseHandler : public EventHandler
{
public:
private:
};



/*
 * Listens for SDL_Events
 * Passes SDL_KeyboardEvents to the Stack of keyHandlers
 * Passes SDL_MouseMotionEvent and SDL_MouseButtonEvent to mouseHandlers
 */
class EventListener
{
public:

private:
	Stack mouseHandlers;
	Stack keyHandlers;
};

}}}